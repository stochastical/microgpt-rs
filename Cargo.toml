[package]
name = "microgpt-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = {version = "0.10.0", features = ["alloc"]}
rand_distr = "0.6.0"
reqwest = { version = "0.13.2", features = ["blocking"] }
